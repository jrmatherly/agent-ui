apiVersion: v1
kind: Service
metadata:
  name: { { include "agent-ui.fullname" . } }
  labels: { { - include "agent-ui.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.targetPort } }
      protocol: TCP
      name: http
  selector: { { - include "agent-ui.selectorLabels" . | nindent 4 } }
