apiVersion: v1
kind: Service
metadata:
  name: agent-ui
  labels:
    app: agent-ui
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: agent-ui
